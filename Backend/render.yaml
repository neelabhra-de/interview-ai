services:
  - type: web
    name: interview-ai-backend
    env: node
    buildCommand: cd Backend && npm install && npx puppeteer browsers install chrome
    startCommand: cd Backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
